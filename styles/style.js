import { StyleSheet, View } from  'react-native';

export const gStyle = StyleSheet.create({
    main: {
        flex: 1,
        padding: 100
    },
    title: {
        fontSize: 20,
        color: '#333',
        fontFamily: 'mt-bold'
    }
})